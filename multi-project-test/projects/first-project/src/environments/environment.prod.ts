import { Environment } from './env';

export const environment: Environment = {
    production: false,
    productApiUrl: 'http://api.PROD.domain.com/product',
    enabledFeatures: ['PROD-feature1']
};
